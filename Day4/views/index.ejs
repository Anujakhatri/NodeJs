<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-800">
        <div class="form p-10 text-white">
            <form action="/create" method="POST">
                <input class="block w-full rounded-md outine-none px-5 py-2 bg-zinc-700" type="text"  placeholder="Title goes here" name="title">
                <textarea class= "block w-full mt-2 px-2 py-2 rounded-md outline-none resize-none bg-zinc-700"  placeholder="Write your task" name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md"  type="submit" value="Create Task">
            </form>
        </div>

        <div class="tasks flex gap-3 flex-wrap p-10">
            <!-- How to handle a file showing path -->
            <% if(files.length >0){ %>
                <% files.forEach(function(val){ %>

                    <div class="task min-w-72 px-3 py-4 rounded-md bg-zinc-700">
                        <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
                        <!-- creating div for edit botton -->
                         <div class="flex w-full justify-between items-center mt-3">
                            <a href="/file/<%=val %>" class="text-blue-500 inline-block mt-3">Read More</a>
                            <a class="text-zinc-500" href="/edit/<%= val %>">edit</a>

                            <!-- Delete form -->
                            <form action="/delete/<%= val %>" method="POST" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete <%= val %>?');">
                                <button type="submit" class="text-red-400" hover:underline >Delete</button>
                            </form>

                         </div>
                        
                    </div> 
                <% }) %>
            
            <% } else { %>
                <h3 class="text-zinc-600">No Taks Yet. </h3>              

            <% } %>            

        </div>
    </div>

    
</body>
</html>