<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= filename %></title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-800 text-white min-h-screen">

    <div class="main w-full p-10">
        <h1 class="text-4xl mb-6">Editing File</h1>

        <form action="/edit/<%= filename %>" method="POST" class="flex flex-col gap-4">
            <!-- Editable filename -->
            <input type="text" name="newFilename" value="<%= filename %>" 
                   class="block w-full rounded-md px-5 py-2 bg-zinc-700 text-white outline-none">

            <!-- Editable content -->
            <textarea name="updates" class="block w-full h-64 rounded-md px-5 py-3 bg-zinc-700 text-white resize-none outline-none"><%= filedata %></textarea>

            <button type="submit" class="mt-2 bg-yellow-600 px-5 py-2 rounded-md w-40 text-white">Save Changes</button>
        </form>

        <div class="mt-10">
            <a href="/" class="text-blue-500 underline">← Back to Home</a>
        </div>
    </div>

</body>
</html>
